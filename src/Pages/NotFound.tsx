import React from 'react';
import PageNotFoundContent from '../components/NotFound/PageNotFoundContent';

const NotFound = () => {
    return (
        <>
            <PageNotFoundContent />
        </>
    );
};

export default NotFound;